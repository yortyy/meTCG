import styles from '@/app/ui/home.module.css';
import { figtree } from '@/app/ui/fonts';
import Image from 'next/image';

export default function Page() {
  return <h1 className={figtree.className}>About Page</h1>;
}
